[project]
name = "visitaid"
version = "1.0.0"
description = "Real-time AI assistant for visually impaired people"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.1",
    "vision-agents[deepgram,elevenlabs,gemini,getstream]>=0.3.8",
]
